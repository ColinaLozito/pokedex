import { baseColors as base_colors, pokemonTypeColors as pokemon_TypeColors } from '../theme/colors'
import { POKEMON_TYPES as pokemonTypes } from '../theme/pokemonTypes'

export const baseColors = {
  ...base_colors,
}

export const pokemonTypeColors = {
  ...pokemon_TypeColors,
}

export const POKEMON_TYPES = {
  ...pokemonTypes
}